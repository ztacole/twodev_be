// import { Router } from 'express';
// import { MajorController } from './major.controller';

// const router = Router();
// const majorController = new MajorController();

// router.get('/', majorController.getAllMajors.bind(majorController));

// router.post('/', majorController.createMajor.bind(majorController));

// router.put('/:id', majorController.updateMajor.bind(majorController));

// router.delete('/:id', majorController.deleteMajor.bind(majorController));

// export default router;